(define (main)
	(set (get 1) 5 10))

; set(f, x, y) = f(x + 5, y + 5)
(define (set f x y)
	(f (+ x 5) (+ y 5)))

; get(x) = +, if x = 0
; get(x) = *
(define (get x)
	(if (= x 0)
		(return +)
		(return *)))
