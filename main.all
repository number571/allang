(include vms
	lib/vms/init.vms)

(include all
	lib/all/lr.all
	lib/all/add.all
	lib/all/sub.all
	lib/all/mul.all)

; result: 120
(define (main)
	(fact 5))

; f(x) = 1, if x < 1
; f(x) = x * f(x-1)
(define (fact x)
	(if (lr x 1) 
		(add 0 1)
		(mul x (fact (sub x 1)))))
